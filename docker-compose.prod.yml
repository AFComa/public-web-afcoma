version: '3.6'

services:
  public-afcoma-prod:
    container_name: public-afcoma-prod
    image: public-afcoma-prod:v0.0.2-prod
    restart: always
    environment:
      - ENV=prod
    build:
      context: .
    ports:
      - '5059'
